<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/events">Wael Arous</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarNav"
    aria-controls="navbarNav"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li  <% if (pagename === 'home') { %>class="nav-item active" <% } %>>
        <a class="nav-link" href="/events">Home </a>
      </li>
      <% if (user) { %>
      <li <% if (pagename === 'create') { %>class="nav-item active" <% } %>>
        <a class="nav-link" href="/events/create">Add Event</a>
      </li>

      <li <% if (pagename === 'profile') { %>class="nav-item active" <% } %>>
        <a class="nav-link" href="/users/profile">Profile</a>
      </li>
    </ul>
    <%} %>
    <ul class="navbar-nav ml-auto">
      <% if (!user) { %>
      <li class="nav-item active">
        <a class="nav-link" href="/users/login"> Login</a>
      </li>

      <li class="nav-item active">
        <a class="nav-link" href="/users/signup"> Sign up</a>
      </li>
      <% } else { %>
      <li class="nav-item active">
        <a class="nav-link" href="/users/logout"> Logout</a>
      </li>
      <%} %>
    </ul>
  </div>
</nav>
